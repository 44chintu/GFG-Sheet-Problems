public void rotate(int arr[], int n)
    {
        int temp = arr[0];
        arr[0] = arr[n-1];
        arr[n-1] = temp;
        
        for(int i=1;i<n;i++){
            temp = arr[i];
            arr[i] = arr[n-1];
            arr[n-1] = temp;
        }
    }
